import React from 'react';
import styled from 'styled-components';

const loginList = styled.div`
  ##padding: 30px 0 30px 0;
`;

const Login = () => {
  return (
    <div>
      <loginList>로그인 페이지입니다.</loginList>
    </div>
  );
};

export default Login;
